Question 1 (1 point)
What is a Markov chain?

Question 1 options:

A chain with a rubber coating

A sequence of nodes in a graph with cycles

A sequence of nodes in a graph without cycles

A mathematical model that experiences transition of states with probabilistic rules

None of these answers

Question 2 (1 point)
What is a Markov Decision Process?

Question 2 options:

A process for making a decision between two options

The underlying logic of a Turing Machine

A process for making a decision between more than two options

An extension of the Markov chain with actions and rewards

None of these answers

Question 3 (1 point)
In a Markov Decision Process, taking an action in a state always leads to the same result state.

Question 3 options:
True
False
Question 4 (1 point)
What is a problem with defining total reward from a starting point to be the sum of all subsequent rewards?

Question 4 options:

The sum of all subsequent rewards might be negative

The sum of all subsequent rewards might be zero

The sum of all subsequent rewards might be positive

None of these answers

The sum of all subsequent rewards might be infinite

Question 5 (1 point)
What is a policy in Reinforcement Learning?

Question 5 options:

A function that specifies what action to take in a certain state

A function that specifies the next state to visit

A function that gives the list all the possible actions in a state

None of these answers

A function that gives the list of all impossible actions in a state

Question 6 (1 point)
What is given by the state value function?

Question 6 options:

It takes a state and gives the expected total reward we can get starting from that state

It takes a state and gives an action that results in the highest reward

None of these answers

It takes an action and a state and gives the expected total reward we can get from taking that action

It takes an action and gives the state that results from taking that action

Question 7 (1 point)
What is the action value function?

Question 7 options:

It takes a state and gives expected total reward we can get starting from that state

It takes a state and an action and gives the state resulting from taking the action

It takes a state and an action and gives the immediate reward resulting from taking that action

It takes a state and an action and gives the expected total reward we can get starting from that state and taking that action

None of these answers

Question 8 (1 point)
What is a greedy policy?

Question 8 options:

A policy that rotates through all actions

A policy that dictates always taking rather than giving

None of these answers

A policy that dictates always taking the action that results in the highest immediate reward

A policy that always specifies the same action

Question 9 (1 point)
What does the Bellman Equation say in the context of Q-Learning?

Question 9 options:

It says that the value of an action a in some state s is the immediate reward you get for taking that action, plus the total past rewards from the previous next state.

It says that the value of taking an action a in some state s is the immediate reward you get for taking that action, minus the maximum expected future rewards you can get in the next state.

It says that the value of taking an action a in some state s is the immediate reward you get for taking that action, plus the minimum expected future rewards you can get in the next state.

None of these answers

It says that the value of taking an action a in some state s is the immediate reward you get for taking that action, plus the maximum expected future rewards you can get in the next state.
